name: Land
description: Land a pull request
inputs:
  base:
    default: main
    description: The base branch to rebase onto
    required: false
  method:
    default: github-pull-request
    description: One of 'ghstack', 'sapling-pull-request', or 'github-pull-request' (required when invoking this action directly)
    required: false
  github-token:
    default: ${{ github.token }}
    description: The GitHub Token
    required: false
  pull-request-url:
    default: ${{ github.event.pull_request.html_url || github.event.issue.pull_request.html_url }}
    description: The Pull Request URL
    required: false
  registry:
    default: nixpkgs
    description: Nix registry to use
    required: false
runs:
  using: composite
  steps:
    - env:
        INPUT_PULL_REQUEST_URL: ${{ inputs.pull-request-url }}
        INPUT_REGISTRY: ${{ inputs.registry }}
      if: inputs.method == 'ghstack'
      run: nix run "$INPUT_REGISTRY#ghstack" -- land "$INPUT_PULL_REQUEST_URL"
      shell: bash
    - env:
        GH_TOKEN: ${{ inputs.github-token }}
        INPUT_BASE: ${{ inputs.base }}
        INPUT_PULL_REQUEST_URL: ${{ inputs.pull-request-url }}
        INPUT_REGISTRY: ${{ inputs.registry }}
      if: inputs.method == 'sapling-pull-request'
      run: |
        nix run "$INPUT_REGISTRY#sapling" -- rebase --dest "$INPUT_BASE"
        nix run "$INPUT_REGISTRY#sapling" -- push --to "$INPUT_BASE"
        DELETE_BRANCH=$(gh pr view "$INPUT_PULL_REQUEST_URL" --json headRefName -q .headRefName)
        nix run "$INPUT_REGISTRY#sapling" -- push --delete "$DELETE_BRANCH"
      shell: bash
    - env:
        GH_TOKEN: ${{ inputs.github-token }}
        INPUT_PULL_REQUEST_URL: ${{ inputs.pull-request-url }}
      if: inputs.method == 'github-pull-request'
      run: |
        gh pr merge \
          --rebase \
          --delete-branch \
          "$INPUT_PULL_REQUEST_URL"
      shell: bash
