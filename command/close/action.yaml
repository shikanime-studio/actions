name: Close Command
description: Close pull request on a pull request head ref
inputs:
  github-token:
    default: ${{ github.token }}
    description: The GitHub Token
    required: false
  permissions:
    default: write,maintain,admin
    description: Permissions to require for the command
    required: false
  pull-request-url:
    default: ${{ github.event.issue.pull_request.html_url }}
    description: The Pull Request URL
    required: false
  reaction:
    default: rocket
    description: Reaction to add to the triggering comment
    required: false
  registry:
    default: nixpkgs
    description: Nix registry to use
    required: false
  username:
    default: github-actions
    description: Username
    required: false
runs:
  using: composite
  steps:
    - id: createGithubAppToken
      uses: actions/create-github-app-token@v3
      with:
        app-id: ${{ vars.OPERATOR_APP_ID }}
        permission-contents: write
        permission-issues: write
        permission-pull-requests: write
        private-key: ${{ secrets.OPERATOR_PRIVATE_KEY }}
    - id: command
      uses: github/command@v2
      with:
        allowed_contexts: pull_request
        command: .close
        github_token: ${{ steps.createGithubAppToken.outputs.token }}
        permissions: ${{ inputs.permissions }}
        reaction: ${{ inputs.reaction }}
        skip_ci: true
    - id: checkout
      if: steps.command.outputs.continue == 'true'
      uses: shikanime-studio/actions/checkout@v9
      with:
        app-slug: ${{ steps.createGithubAppToken.outputs.app-slug }}
        github-token: ${{ steps.createGithubAppToken.outputs.token }}
        pull-request-url: ${{ inputs.pull-request-url }}
        registry: ${{ inputs.registry }}
        username: ${{ inputs.username }}
    - id: close
      continue-on-error: true
      if: steps.command.outputs.continue == 'true'
      uses: shikanime-studio/actions/close@v9
      with:
        github-token: ${{ steps.createGithubAppToken.outputs.token }}
        method: ${{ steps.checkout.outputs.method }}
        pull-request-url: ${{ inputs.pull-request-url }}
        registry: ${{ inputs.registry }}
    - env:
        GH_TOKEN: ${{ steps.createGithubAppToken.outputs.token }}
        INPUT_PULL_REQUEST_URL: ${{ inputs.pull-request-url }}
      if: steps.close.outcome == 'failure'
      run: |
        PR_URL="$INPUT_PULL_REQUEST_URL"

        IFS=' ' read -r SCHEME HOST OWNER REPO <<<"$(
          jq -Rr 'capture("^(?<scheme>https?)://(?<host>[^/]+)/(?<owner>[^/]+)/(?<repo>[^/]+)/pull/(?<number>[0-9]+)") | "\(.scheme) \(.host) \(.owner) \(.repo)"' \
            <<<"$PR_URL"
        )"

        RUN_URL="$SCHEME://$HOST/$OWNER/$REPO/actions/runs/${{ github.run_id }}"

        BODY="$(cat <<EOF
        ### ⚠️ Cannot proceed with operation

        - logs: $RUN_URL

        > Close failed
        EOF
        )"

        gh pr comment "$PR_URL" --body "$BODY"
        exit 1
      shell: bash
